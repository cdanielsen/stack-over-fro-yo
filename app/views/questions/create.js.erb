$(".errors").empty();
$("#new_question").show();
var index = parseInt($(".unbullet li:last-child").attr('id')) + 1;
$(".unbullet").append('<li>' + '<a href="<%= question_path(@question) %>">' + index + '</a>' + ": " + '<%= @question.headline %>' + '</li>');
$("form").hide();
